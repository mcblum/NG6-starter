import template from './mainFooter.component.html';
import controller from './mainFooter.controller';
import './mainFooter.scss';

let mainFooterComponent = {
  restrict: 'E',
  bindings: {},
  transclude: {},
  template,
  controller
};

export default mainFooterComponent;
